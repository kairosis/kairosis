{
  "name": "events-core",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "events/core/src",
  "projectType": "library",
  "tags": ["publishable"],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/events/core",
        "main": "events/core/src/index.ts",
        "tsConfig": "events/core/tsconfig.lib.json",
        "assets": ["events/core/*.md"]
      }
    }
  }
}
